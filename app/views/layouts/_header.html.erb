<header>
  <%= image_tag("logo.png", alt: "Slide Annotator", class: "round") %>
  <nav class="round">
    <ul>
      <li>
        <%= form_tag searchresults_path, :method => 'get' do %>
          <%= text_field_tag :search, params[:search] %>
          <%= submit_tag "Search", :name => nil %>
        <% end %>
      </li>
      <li><%= link_to "Slidesets", slidesets_path %></li>
    </ul>
  </nav>
</header>
